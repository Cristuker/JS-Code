const { Deque } = require("./deque");

function palindromeChecker(aString) {
    if (!aString) return false;

    const deque = new Deque();
    const lowerString = aString.toLocaleLoweCase()

}

palindromeChecker(null)
palindromeChecker(undefined)
palindromeChecker('')
palindromeChecker(1)